{
  "name": "database-visualiser",
  "version": "1.0.0",
  "description": "Browser-based SQL teaching tool: parses DDL and renders the schema as an interactive horizontal tree.",
  "scripts": {
    "start": "parcel serve src/index.html --public-url / --dist-dir dist",
    "test": "mocha",
    "build": "parcel build --public-url ./ ./src/index.html --dist-dir build"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "anim-event": "^1.0.17",
    "bootstrap": "^5.2.3",
    "js-tokens": "^8.0.1",
    "leader-line-new": "^1.1.9"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.4",
    "parcel": "^2.16.4"
  }
}
